import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    padding: 10,
  },
  imageStyle: {
    width: 80,
    height: 80,
    borderRadius: 40,
  },
});

export default styles;
