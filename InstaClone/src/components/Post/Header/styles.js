import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    alignItems: 'center',
    flexDirection: 'row',
  },
  name: {
    color: '#3c3c3c',
    fontWeight: 'bold',
  },
});

export default styles;
